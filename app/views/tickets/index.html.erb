<table>
  <tr>
    <th>Status</th>
    <th>Kategoria</th>
    <th>Pracownik</th>
    <th>E-mail klienta</th>
    <th>Numer zamówienia</th>
  </tr>

  <% for ticket in @tickets %>
    <tr>
      <td><%=h ticket.basic_state %></td>
      <td><%=unless ticket.category.nil?
               h ticket.category.name
             else
               "Błędna kategoria!"
             end
          %></td>
      <td><%=h ticket.employee_name %></td>
      <td><%=h ticket.email %></td>
      <td><%=h ticket.order_number %></td>
      <td><%= link_to 'Pokaż', ticket %> | <%= link_to 'Edytuj', edit_ticket_path(ticket) %> | 
        <%= link_to 'Usuń', ticket, :confirm => 'Jesteś pewny?', :method => :delete %></td>
    </tr>
  <% end %>
</table>
