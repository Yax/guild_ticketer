<h1>Edycja kategorii zgłoszeń</h1>

<% form_for [:admin, @ticket_category] do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :ticket_type %>
    <%= f.select :ticket_type, TICKET_TYPES.collect { |c| [ c.capitalize, c ] } %>
  </p>
  <p>
    <%= f.submit "Zapisz" %>
  </p>
<% end %>

<%= link_to 'Pokaż', [:admin,@ticket_category] %> |
<%= link_to 'Wstecz', admin_ticket_categories_path %>
